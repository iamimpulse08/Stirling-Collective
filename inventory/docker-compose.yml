version: "3.8"

services:
  db:
    image: postgres:alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
  app:
    build: .
    env_file:
      - .env
    depends_on:
      - postgres